import { Alert } from "react-bootstrap";

const Footer = () => {
    return (
        <Alert className="text-center" variant="info">Công ty TNHH MTV Tổ chức tour du lịch Đồng Minh</Alert>
    );
}

export default Footer;